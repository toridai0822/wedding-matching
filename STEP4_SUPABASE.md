# ステップ4: Supabaseでデータベースを作る（15分）

Supabaseは「データを保存する場所」です。参加者情報とマッチング結果を保存します。無料で使えます。

## 4-1. Supabaseのサイトに行く

🌐 https://supabase.com にアクセス

## 4-2. アカウントを作る

1. 「Start your project」をクリック
2. 「Sign in with GitHub」を選択
   - GitHubアカウントでログイン
   - 「Authorize Supabase」をクリック

**完了！** Supabaseにログインできました。

---

## 4-3. 新しいプロジェクトを作る

1. 「New project」をクリック

### 入力する情報

| 項目 | 入力内容 |
|------|----------|
| Name | `wedding-matching` |
| Database Password | **強力なパスワード**を作る<br>（メモ帳に保存しておく！） |
| Region | `Northeast Asia (Tokyo)` を選択 |
| Pricing Plan | **Free** を選択 |

2. 「Create new project」をクリック

**2〜3分待つと完成します！**

---

## 4-4. データベースのテーブルを作る

### 4-4-1. 参加者テーブル（participants）を作る

1. 左側のメニューから「Table Editor」をクリック
2. 「Create a new table」をクリック

#### 設定

| 項目 | 入力内容 |
|------|----------|
| Name | `participants` |
| Description | 参加者情報 |
| Enable Row Level Security (RLS) | ❌ チェックを外す |

#### カラム（列）を追加

デフォルトの `id` はそのままで、以下を追加：

| Column Name | Type | Default Value | Primary | Not Null |
|-------------|------|---------------|---------|----------|
| id | uuid | gen_random_uuid() | ✅ | ✅ |
| reception_number | int4 | | | ✅ |
| gender | text | | | ✅ |
| first_choice | int4 | | | ✅ |
| second_choice | int4 | | | ✅ |
| name | text | | | |
| created_at | timestamptz | now() | | ✅ |

3. 「Save」をクリック

---

### 4-4-2. マッチングテーブル（matches）を作る

1. また「Create a new table」をクリック

#### 設定

| 項目 | 入力内容 |
|------|----------|
| Name | `matches` |
| Description | マッチング結果 |
| Enable Row Level Security (RLS) | ❌ チェックを外す |

#### カラム（列）を追加

| Column Name | Type | Default Value | Primary | Not Null |
|-------------|------|---------------|---------|----------|
| id | uuid | gen_random_uuid() | ✅ | ✅ |
| participant1_number | int4 | | | ✅ |
| participant2_number | int4 | | | ✅ |
| match_type | text | | | ✅ |
| match_priority | int4 | | | ✅ |
| created_at | timestamptz | now() | | ✅ |

3. 「Save」をクリック

---

## 4-5. APIキーをコピーする

JavaScriptからデータベースに接続するための「鍵」をコピーします。

1. 左側のメニューから「Settings」（歯車アイコン）をクリック
2. 「API」をクリック

### コピーする情報（メモ帳に保存）

#### ① Project URL
```
例: https://abcdefghijk.supabase.co
```

#### ② anon public key
```
例: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSI...
（とても長い文字列）
```

**この2つを絶対にメモ帳に保存してください！**

---

✅ ステップ4終了！データベースの準備ができました！

次はJavaScriptのコードを書き換えて、Supabaseに接続します。